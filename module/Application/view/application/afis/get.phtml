<?php 
if ($admin)
{
	foreach ($afises as $afis) {
		echo $this->afViewHelper()->renderAdminRow($afis);
	}
} 
else 
{
	foreach ($afises as $afis) {
		echo $this->afViewHelper()->renderRow($afis);		
		// echo $this->afViewHelper()->renderRow($afis, $notams);
	}	
}