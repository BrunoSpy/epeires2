<div id="event">
<div id="loading">Chargement...</div>
<!-- formulaire createform.phtml récupéré via ajax -->
</div>
<?php $this->inlineScript()->captureStart();?>
$("#loading").ajaxStart(function(){
	$(this).show();})
	.ajaxStop(function(){
	$(this).hide();});
	
	
$("#create-link").on("click", function(){

	$("#event").load(
		'<?php echo $this->url('application', array('controller'=>'events', 'action'=>'createform'))?>'
	);
	
	loadModernizr();
});

//TODO : trouver un moyen de mettre ce code dans createform.phtml
$(document).on("blur", "#name", function(){
	$.post(
	'<?php echo $this->url('application',
							array('controller'=>'events', 'action'=>'createform'),
							array('query'=>array('type'=>'category')) ) ?>',
	function(data){
		$("#category").html(data);
	});
});

<?php $this->inlineScript()->captureEnd();?>