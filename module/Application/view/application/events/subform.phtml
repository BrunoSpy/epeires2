<?php
$this->form->prepare();
switch ($this->part) {
    case 'subcategories':
        echo '<option value="-1">Autre</option>';
        foreach ($this->values as $key => $value) {
            echo "<option value=\"" . $key . "\">" . $value . "</option>";
        }
        ;
        break;
    case 'predefined_events':
        if (empty($this->values)) {
            echo '<div class="col-sm-10">';
            echo '<p>Aucun modèle défini.</p>';
            echo '</div>';
        } else {
            echo '<div class="col-md-6">';
            
            $count = count($values);
            $i = 1;
            $separated = false;
            foreach ($this->values as $key => $value) {
                echo '<div class="container-fluid">';
                    echo '<div class="models-col-1 hidden-xs">';
                        echo '<div class="round round-lg grey"><i class="mdi-content-content-paste"></i></div>';
                    echo '</div>';
                    echo '<div class="models-col-2">';
                    echo '<p> '.$value.'</p>';
                    echo '</div>';
                    echo '<div class="models-col-3">';
                    echo '<span class="pull-right"><button data-id="'.$key.'" class="btn btn-sm btn-primary">Utiliser</button></span>';
                    echo ' </div>';
                echo '</div>';
                if ($i >= $count / 2 && ! $separated) {
                    echo '</div>';
                    echo '<div class="col-md-6">';
                    $separated = true;
                }
                $i ++;
            }
            
            echo '</div>';
        }
        break;
    case 'custom_fields':
        foreach ($this->form->get('custom_fields')->getElements() as $element) {
            echo $this->customFieldGroup($element, 'form-control');
        }
        break;
    default:
        break;
}